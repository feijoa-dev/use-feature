name: Pull Request

on:
  push:
    branches: 
      - '*'
      - '!main'
  pull_request:
    branches: 
      - '*'
      - '!main'

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - name: Coverage Report
        if: github.actor != 'dependabot[bot]'
        uses: artiomtr/jest-coverage-report-action@Debug-action
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          test-script: npm run test:unit --  --json --outputFile ./report.json
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          REACT_APP_SHOW_FEATURE_1: true
          GATSBY_FEATURE_3: true
          OTHER_FEATURE_1: true
          REACT_APP_SHOW_FEATURE_2: false
        
      - run: npm run build
      - run: npm publish --access public --dry-run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}